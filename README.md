Note:
to load this plugin, user must open `chrome://flags`

Enable the option

> Extensions on chrome:// URLs
> Enables running extensions on chrome:// URLs, where extensions explicitly request this permission. – Mac, Windows, Linux, ChromeOS, Android
> #extensions-on-chrome-urls

Otherwise:
The extension will unable to load with error message `invalid scheme`.
